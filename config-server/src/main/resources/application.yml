
server:
  port: 7000
#服务名字
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          #git 仓库的地址
          uri: https://gitee.com/tbh0ghy9/config.git
          #git 仓库的账号密码
          username: tbh0ghy9
          password: daaijmd3344..
          #搜索路径
          search-paths: /config/
          clone-on-start: true
